<!-- <?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="friends.mapper.UserMapper">

	<insert id="join">
		INSERT INTO user
		(
			id,
			pw,
			name
		) VALUES (
			#{id},
			#{pw},
			#{name}
		);			
	</insert>
	<select id="idCheck" resultType="java.lang.String">
		SELECT 
			id 
		FROM
			user
		WHERE 
			id = #{id};				
	</select>
	<select id="pwCheck" resultType="java.lang.String">
		SELECT 
			pw 
		FROM
			user
		WHERE 
			id = #{id};				
	</select>
	<select id="name" resultType="java.lang.String">
		SELECT 
			name 
		FROM
			user
		WHERE 
			id = #{id};				
	</select>

	
</mapper>
	 -->